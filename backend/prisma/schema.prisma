generator client {
  provider = "prisma-client-js"
  output   = "./generated/prisma-client"
  moduleFormat = "cjs"
}

datasource db {
  provider = "postgresql"
}

model User {
  id           Int    @id @default(autoincrement())
  email        String @unique
  passwordHash String
}
